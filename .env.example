# Names only — never commit values.
# Copy by NAME from closest photo+lead kiosk on Vercel
# (turespana-imex-kiosk or elevate-photobooth-2).
# This kiosk does NOT require GEMINI_API_KEY.
# Set FROM_NAME to RPB Law Firm after copy (not a secret).

OPENAI_API_KEY=
OPENAI_IMAGE_MODEL=
OPENAI_IMAGE_SIZE=
OPENAI_IMAGE_QUALITY=
GMAIL_USER=
GOOGLE_APP_PASSWORD=
WYZER_GMAIL_USER=
WYZER_APP_PASSWORD=
FROM_NAME=
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_BUCKET=
